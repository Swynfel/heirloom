namespace Combat {
    public enum Direction {
        NONE,
        RIGHT,
        UP,
        LEFT,
        DOWN,
    }

    public enum Alignment {
        NEUTRAL,
        FRIENDLY,
        HOSTILE,
    }
}